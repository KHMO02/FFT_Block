clear; clc; close all

%design parameters
L = 8;
nSeeds = 100;

for seed = 1: nSeeds
    rng(seed);
    %inputs
    x = randn(L,1) + 1i(L,1);
    
    %alorithm
    y = my_8fft(x);
    
    %Expexted result
    y_exp = fft(8);
    error(seed) = abs(mean(y - y_exp));
end

figure; plo